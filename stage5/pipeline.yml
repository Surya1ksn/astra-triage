# DONE -- superseded by .github/workflows/ci.yml, which is the actual
# active pipeline GitHub Actions runs. This file is kept as the original
# draft/skeleton for reference; edit .github/workflows/ci.yml, not this
# file, if the pipeline needs to change.
#
# Required behavior (all satisfied in .github/workflows/ci.yml):
#   - Run lint + unit tests on every push/PR.
#   - Run stage4/evaluation.py and FAIL the pipeline if it exits non-zero
#     (i.e. below the quality thresholds in golden_set.json).
#   - A separate `deploy` job depends on `test` AND `evaluate` both
#     succeeding — deployment is impossible if evaluation fails.
#   - Never print or echo secrets. API keys come from repo/CI secrets,
#     referenced by name, never inlined.

### See .github/workflows/ci.yml for the implemented pipeline. ###
#
# Not duplicated here to avoid the two files drifting out of sync --
# this comment intentionally replaces what was originally a full second
# copy of the workflow.
